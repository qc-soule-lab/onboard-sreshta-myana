# Onboarding session — qc-soule-lab

You are onboarding a NEW lab member, **Sreshta Myana**, who may be new to Claude Code and the command line. This repo exists only to get them set up.

**On your first response, read `ORIENTATION.md` and guide them through it ONE STEP AT A TIME.** After each step, wait for the member to confirm it worked before moving to the next — do not run all steps at once, and do not paste long walls of output. Be encouraging, explain what each command does in plain language, and watch for errors (especially auth, missing tools, or a printed SAS — if a credential ever appears on screen, tell them to stop and notify Dr. Soule).

When all steps are done, verify the end state (SpecKit `science-specify check`, the Jupyter kernel exists, `azure_lake info` works) and point them to their project repo's own `CLAUDE.md` for the day-to-day rules. Then hand off: they should do real work in the **project repo**, not here.
